const {BrowserWindow} = require('electron'),
path = require('path'),
url = require('url')


function ventana(pathFile, medidas){
    let
        this.loadFile(url.format({
            pathname:path.join(__dirname, pathFile)
        }))
        console.log('INICIO')
    }
}
module.exports = Ventana